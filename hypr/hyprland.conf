monitor=,preferred,auto,1

exec-once = waybar & swaync
exec-once = wl-paste --type text --watch cliphist store
exec-once = wl-paste --type image --watch cliphist store
exec-once = hyprctl setcursor WinSur-white-cursors 38
# exec-once = swaybg --image /mnt/data/lt/res/wallpaper/03.jpg
# exec-once = mpvpaper -o "loop no-audio" '*' /mnt/data/lt/res/wallpaper/mylivewallpapers-com-Night-Field-Elden-Ring-FHD.mp4
# exec-once = [workspace 9 silent] qbittorrent 
# exec-once = /usr/lib/polkit-gnome/polkit-gnome-authentication-agent-1

# Source a file (multi-file configs)
# source = ~/.config/hypr/myColors.conf

# Some default env vars.
env = XCURSOR_SIZE,38
env = XCURSOR_PATH,/usr/share/icons:~/.local/share/icons
env = XCURSOR_THEME,WinSur-white-cursors
env = HYPRCURSOR_THEME,WinSur-white-cursors
env = HYPRCURSOR_SIZE,38
env = EDITOR,nvim
env = PAGER,subl
env = QT_SCALE_FACTOR,1.5
# env = QT_AUTO_SCREEN_SCALE_FACTOR,1
# env = QT_ENABLE_HIGHDPI_SCALING,1
env = ELM_SCALE,1.5
env = GDK_SCALE,2 # only int
env = GDK_DPI_SCALE,1.5 # for text only
env = RUSTICL_ENABLE,radeonsi
env = HYPRSHOT_DIR,/mnt/data/inbox

input {
    kb_layout = us,ru
    kb_variant =
    kb_model =
    kb_options = grp:caps_select
    # варианты ниже, оказались неудачными
    # kb_options = ctrl:swapcaps,grp:ctrl_shift_toggle
    # kb_options = grp:ctrl_shift_toggle
    # kb_options = grp:alt_shift_toggle
    # kb_options = ctrl:nocaps,grp:toggle
    # kb_options = ctrl:nocaps,grp:ctrl_space_toggle

    kb_rules =
    repeat_rate = 50
    repeat_delay = 250

    follow_mouse = 1
    mouse_refocus = false
    touchpad {
        natural_scroll = no
    }

    sensitivity = 0 # -1.0 - 1.0, 0 means no modification.
}


general {
    gaps_in = 5
    gaps_out = 20
    border_size = 2
    col.active_border = rgba(33ccffee) rgba(00ff99ee) 45deg
    col.inactive_border = rgba(595959aa)
    # Set to true enable resizing windows by clicking and dragging on borders and gaps
    resize_on_border = false
    allow_tearing = false
    layout = dwindle
}

decoration {
    rounding = 10
    rounding_power = 2
    # Change transparency of focused and unfocused windows
    active_opacity = 1.0
    inactive_opacity = 1.0

    shadow {
        enabled = true
        range = 4
        render_power = 3
        color = rgba(1a1a1aee)
    }

    blur {
        enabled = true
        size = 3
        passes = 1
        vibrancy = 0.1696
    }
}

animations {
    enabled = yes, please :)
    # Default curves, see https://wiki.hypr.land/Configuring/Animations/#curves
    #        NAME,           X0,   Y0,   X1,   Y1
    bezier = easeOutQuint,   0.23, 1,    0.32, 1
    bezier = easeInOutCubic, 0.65, 0.05, 0.36, 1
    bezier = linear,         0,    0,    1,    1
    bezier = almostLinear,   0.5,  0.5,  0.75, 1
    bezier = quick,          0.15, 0,    0.1,  1

    # Default animations, see https://wiki.hypr.land/Configuring/Animations/
    #           NAME,          ONOFF, SPEED, CURVE,        [STYLE]
    animation = global,        1,     10,    default
    animation = border,        1,     5.39,  easeOutQuint
    animation = windows,       1,     4.79,  easeOutQuint
    animation = windowsIn,     1,     4.1,   easeOutQuint, popin 87%
    animation = windowsOut,    1,     1.49,  linear,       popin 87%
    animation = fadeIn,        1,     1.73,  almostLinear
    animation = fadeOut,       1,     1.46,  almostLinear
    animation = fade,          1,     3.03,  quick
    animation = layers,        1,     3.81,  easeOutQuint
    animation = layersIn,      1,     4,     easeOutQuint, fade
    animation = layersOut,     1,     1.5,   linear,       fade
    animation = fadeLayersIn,  1,     1.79,  almostLinear
    animation = fadeLayersOut, 1,     1.39,  almostLinear
    animation = workspaces,    1,     1.94,  almostLinear, fade
    animation = workspacesIn,  1,     1.21,  almostLinear, fade
    animation = workspacesOut, 1,     1.94,  almostLinear, fade
    animation = zoomFactor,    1,     7,     quick
}

dwindle {
    pseudotile = true # Master switch for pseudotiling. Enabling is bound to mainMod + P in the keybinds section below
    preserve_split = true # You probably want this
}

master {
    new_status = master
}

misc {
    force_default_wallpaper = -1 # Set to 0 or 1 to disable the anime mascot wallpapers
    disable_hyprland_logo = false # If true disables the random hyprland logo / anime girl background. :(
}


windowrulev2 = float,size 20% 40%,title:(SpeedCrunch)
windowrulev2 = float, class:^(floating)$
windowrulev2 = size 20% 40%, class:^(floating)$
# windowrule = move cursor -50% -50%, ^(floating)$
# windowrulev2 = workspace name:E, class:^(obsidian)
$mainMod = SUPER

# bind = $mainMod, RETURN, exec, alacritty # 256 colors
# bind = $mainMod, U, exec, [workspace name:1] thorium-browser
# bind = $mainMod, U, exec, google-chrome-stable --force-device-scale-factor=2
# bind = $mainMod, S, exec, env SDL_VIDEODRIVER=wayland steam
# bind = $mainMod, Y, exec, kitty -e mc
# bind = $mainMod, C, exec, speedcrunch --class floating
# bind = $mainMod, C, exec, kitty --class floating -e python

bind = $mainMod, Q, exec, wofi --show
bind = $mainMod, W, exec, firefox
bind = $mainMod, E, exec, env QT_SCALE_FACTOR=1.5 doublecmd 
bind = $mainMod, A, fullscreen
bind = $mainMod, S, togglesplit #, dwindle
bind = $mainMod, D, killactive
bind = $mainMod, Z, togglefloating
bind = $mainMod, X, exec, obsidian
bind = $mainMod, C, exec, speedcrunch
bind = $mainMod, V, exec, cliphist list | wofi --dmenu | cliphist decode | wl-copy
bind = $mainMod, B, exec, /mnt/data/lt/dotf/bookmark/bookmark.sh
bind = $mainMod, space, exec, kitty # 24bit colors
bind = $mainMod Shift, L, exit
bind = , PRINT, exec, hyprshot -m region
bind = $mainMod, P, pseudo #, dwindle

# Move focus with mainMod + arrow keys
bind = $mainMod, H, movefocus, l
bind = $mainMod, L, movefocus, r
bind = $mainMod, K, movefocus, u
bind = $mainMod, J, movefocus, d

bind = $mainMod, left, movefocus, l
bind = $mainMod, right, movefocus, r
bind = $mainMod, up, movefocus, u
bind = $mainMod, down, movefocus, d

# Switch workspaces with mainMod + [0-9]
bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 5, workspace, 5
bind = $mainMod, 6, workspace, 6
bind = $mainMod, 7, workspace, 7
bind = $mainMod, 8, workspace, 8
bind = $mainMod, 9, workspace, 9
bind = $mainMod, 0, workspace, 10

# Move active window to a workspace with mainMod + SHIFT + [0-9]
bind = $mainMod SHIFT, 1, movetoworkspace, 1
bind = $mainMod SHIFT, 2, movetoworkspace, 2
bind = $mainMod SHIFT, 3, movetoworkspace, 3
bind = $mainMod SHIFT, 4, movetoworkspace, 4
bind = $mainMod SHIFT, 5, movetoworkspace, 5
bind = $mainMod SHIFT, 6, movetoworkspace, 6
bind = $mainMod SHIFT, 7, movetoworkspace, 7
bind = $mainMod SHIFT, 8, movetoworkspace, 8
bind = $mainMod SHIFT, 9, movetoworkspace, 9
bind = $mainMod SHIFT, 0, movetoworkspace, 10

# Scroll through existing workspaces with mainMod + scroll
bind = $mainMod, mouse_down, workspace, e+1
bind = $mainMod, mouse_up, workspace, e-1

# Move/resize windows with mainMod + LMB/RMB and dragging
bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow
