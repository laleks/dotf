# autogenerated = 1 # remove this line to remove the warning

# See https://wiki.hyprland.org/Configuring/Monitors/
monitor=,preferred,auto,1

# See https://wiki.hyprland.org/Configuring/Keywords/ for more

# Execute your favorite apps at launch
# exec-once = waybar & hyprpaper & firefox
exec-once = waybar & swaync
exec-once = wl-paste --type text --watch cliphist store
exec-once = wl-paste --type image --watch cliphist store
# exec-once = swaybg --image /mnt/data/lt/res/wallpaper/03.jpg
# exec-once = mpvpaper -o "loop no-audio" '*' /mnt/data/lt/res/wallpaper/mylivewallpapers-com-Night-Field-Elden-Ring-FHD.mp4

#exec-once = [workspace 9 silent] qbittorrent 
#exec-once = /usr/lib/polkit-gnome/polkit-gnome-authentication-agent-1

# Source a file (multi-file configs)
# source = ~/.config/hypr/myColors.conf

# Some default env vars.
env = XCURSOR_SIZE,32
env = HYPRCURSOR_SIZE,28
env = QT_SCALE_FACTOR,1.5
env = GDK_DPI_SCALE,1.5
env = GDK_SCALE,1.5
env = EDITOR,nvim
env = PAGER,subl
env = HYPRCURSOR_THEME,rose-pine-hyprcursor


# For all categories, see https://wiki.hyprland.org/Configuring/Variables/
input {
    kb_layout = us,ru
    kb_variant =
    kb_model =
    kb_options = grp:caps_select # caps:escape
    kb_rules =
    repeat_rate = 50
    repeat_delay = 250

    follow_mouse = 1
    mouse_refocus = false
    touchpad {
        natural_scroll = no
    }

    sensitivity = 0 # -1.0 - 1.0, 0 means no modification.
}

general {
    # See https://wiki.hyprland.org/Configuring/Variables/ for more

    gaps_in = 5
    gaps_out = 15
    border_size = 2
    col.active_border = rgba(33ccffee) rgba(00ff99ee) 45deg
    col.inactive_border = rgba(595959aa)
    layout = dwindle
}

decoration {
    # See https://wiki.hyprland.org/Configuring/Variables/ for more

    rounding = 10
    #blur = yes
    #blur_size = 3
    #blur_passes = 1
    #blur_new_optimizations = on

    # drop_shadow = yes
    # shadow_range = 4
    # shadow_render_power = 3
    # col.shadow = rgba(1a1a1aee)
}

animations {
    enabled = yes

    # Some default animations, see https://wiki.hyprland.org/Configuring/Animations/ for more

    bezier = myBezier, 0.05, 0.9, 0.1, 1.05

    animation = windows, 1, 7, myBezier
    animation = windowsOut, 1, 7, default, popin 80%
    animation = border, 1, 10, default
    animation = borderangle, 1, 8, default
    animation = fade, 1, 7, default
    animation = workspaces, 1, 6, default
}

dwindle {
    # See https://wiki.hyprland.org/Configuring/Dwindle-Layout/ for more
    pseudotile = yes # master switch for pseudotiling. Enabling is bound to mainMod + P in the keybinds section below
    preserve_split = yes # you probably want this
}

# master {
#     # See https://wiki.hyprland.org/Configuring/Master-Layout/ for more
#     new_is_master = true
# }

# gestures {
#     # See https://wiki.hyprland.org/Configuring/Variables/ for more
#     workspace_swipe = off
# }

# Example per-device config
# See https://wiki.hyprland.org/Configuring/Keywords/#executing for more
# device:epic-mouse-v1 {
#     sensitivity = -0.5
# }

# Example windowrule v1
# windowrule = float, ^(kitty)$
# Example windowrule v2
# windowrulev2 = float,class:^(kitty)$,title:^(kitty)$
# See https://wiki.hyprland.org/Configuring/Window-Rules/ for more

windowrulev2 = float,title:(rofi - calc)

# windowrulev2 = workspace name:E, class:(obsidian)

# See https://wiki.hyprland.org/Configuring/Keywords/ for more
# $mainMod = ALT
$mainMod = SUPER

# Example binds, see https://wiki.hyprland.org/Configuring/Binds/ for more
# bind = $mainMod, RETURN, exec, alacritty # 256 colors
bind = $mainMod, RETURN, exec, kitty # 24bit colors
bind = $mainMod, U, exec, [workspace name:1] thorium-browser
bind = $mainMod, B, exec, [workspace name:E] obsidian
bind = $mainMod, X, killactive
bind = $mainMod, M, exec, /mnt/data/lt/dotfiles/bookmark/bookmark.sh
bind = $mainMod Shift, L, exit
bind = $mainMod Shift, T, exec, waveterm
bind = $mainMod, Y, exec, [workspace name:Q] kitty -e yazi
bind = $mainMod, V, exec, cliphist list | wofi --dmenu | cliphist decode | wl-copy
bind = $mainMod, T, togglefloating
bind = $mainMod, F, fullscreen
bind = $mainMod, Z, togglesplit #, dwindle
bind = $mainMod, P, pseudo #, dwindle
bind = $mainMod, O, exec, wofi --show
bind = $mainMod, C, exec, rofi -show calc -modi calc -no-show-match -no-sort
bind = , PRINT, exec, hyprshot -m output --clipboard-only

# Move focus with mainMod + arrow keys
bind = $mainMod, H, movefocus, l
bind = $mainMod, L, movefocus, r
bind = $mainMod, K, movefocus, u
bind = $mainMod, J, movefocus, d

bind = $mainMod, left, movefocus, l
bind = $mainMod, right, movefocus, r
bind = $mainMod, up, movefocus, u
bind = $mainMod, down, movefocus, d

# Switch workspaces with mainMod + [0-9]
bind = $mainMod, 1, workspace, name:1
bind = $mainMod, 2, workspace, name:2
bind = $mainMod, 3, workspace, name:3
bind = $mainMod, Q, workspace, name:Q
bind = $mainMod, W, workspace, name:W
bind = $mainMod, E, workspace, name:E
bind = $mainMod, A, workspace, name:A
bind = $mainMod, S, workspace, name:S
bind = $mainMod, D, workspace, name:D
# bind = $mainMod, Z, workspace, 10

# Move active window to a workspace with mainMod + SHIFT + [0-9]
bind = $mainMod SHIFT, 1, movetoworkspace, name:1
bind = $mainMod SHIFT, 2, movetoworkspace, name:2
bind = $mainMod SHIFT, 3, movetoworkspace, name:3
bind = $mainMod SHIFT, Q, movetoworkspace, name:Q
bind = $mainMod SHIFT, W, movetoworkspace, name:W
bind = $mainMod SHIFT, E, movetoworkspace, name:E
bind = $mainMod SHIFT, A, movetoworkspace, name:A
bind = $mainMod SHIFT, S, movetoworkspace, name:S
bind = $mainMod SHIFT, D, movetoworkspace, name:D
# bind = $mainMod SHIFT, Z, movetoworkspace, 10

# Scroll through existing workspaces with mainMod + scroll
bind = $mainMod, mouse_down, workspace, e+1
bind = $mainMod, mouse_up, workspace, e-1

# Move/resize windows with mainMod + LMB/RMB and dragging
bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow
